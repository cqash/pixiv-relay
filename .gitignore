# 本地便携 Go 工具链（不入库）
.toolchain/

# 构建产物
bin/
*.exe

# 环境配置
.env

# 运行数据（DB / 图片缓存 / 恢复临时文件）
data/

# Web 前端构建产物（独立工程产出，构建时拷入；保留占位 index.html 供 go:embed 编译）
internal/web/dist/
!internal/web/dist/index.html
